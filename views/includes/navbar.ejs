<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SwapIT</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">
    <link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A=="
      crossorigin="anonymous" referrerpolicy="no-referrer" />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"></script>
    <style>
        /* Navbar Styles */
        .navbar {
            background: linear-gradient(90deg, rgba(0, 123, 255, 0.9) 0%, rgba(0, 123, 255, 0.7) 100%);
            padding: 0.5rem 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 1.75rem;
            color: #fff;
            transition: color 0.3s ease;
        }

        .navbar-brand:hover {
            color: #e9ecef;
        }

        .navbar-nav .nav-link {
            color: #fff;
            font-size: 1rem;
            margin: 0 1rem;
            transition: color 0.3s ease, border-bottom 0.3s ease;
        }

        .navbar-nav .nav-link:hover {
            color: #e9ecef;
            border-bottom: 2px solid #e9ecef;
        }

        .navbar-toggler {
            border: none;
            background: none;
        }

        .dropdown-menu {
            border-radius: 0.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border: none;
        }

        .dropdown-item {
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .dropdown-item:hover {
            background-color: rgba(0, 123, 255, 0.1);
            color: #007bff;
        }

        .form-control {
            border: 1px solid rgba(0, 0, 0, 0.2);
            border-radius: 0.25rem;
            transition: border-color 0.3s ease;
        }

        .form-control:focus {
            border-color: #007bff;
            box-shadow: none;
        }

        .btn-outline-success {
            border-color: #28a745;
            color: #28a745;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .btn-outline-success:hover {
            background-color: #28a745;
            color: #fff;
        }

        @media (max-width: 991px) {
            .navbar-nav {
                margin-top: 1rem;
            }

            .navbar-nav .nav-link {
                margin: 0.5rem 0;
            }

            .dropdown-menu {
                text-align: center;
            }

            .form-control, .btn-outline-success {
                width: 100%;
                margin: 0.5rem 0;
            }
        }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
      <div class="container">
        <!-- Logo on the extreme left -->
        <a class="navbar-brand" href="/listing">SwapIT.in</a>

        <!-- Toggler for collapsing menu on small screens -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
          data-bs-target="#navbarNav" aria-controls="navbarNav"
          aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Main navigation links and search form -->
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav mx-auto">
            <li class="nav-item">
              <a class="nav-link" href="/listing">EXPLORE</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/listing/new"><i
                  class="fa-solid fa-plus"></i> RENT</a>
            </li>
          </ul>

          <!-- Search Form for large screens -->
          <form class="d-flex ms-3 d-none d-lg-flex" role="search">
            <input class="form-control me-2" type="search" placeholder="Search"
              aria-label="Search">
            <button class="btn btn-light btn-outline-success"
              type="submit">Search</button>
          </form>

          <!-- Login, Logout, Signup on the extreme right -->
          <div class="d-flex align-items-center d-none d-lg-flex">
            <ul class="navbar-nav">
              <% if (!myUsr) { %>
              <li class="nav-item">
                <a class="nav-link" href="/login">Login</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/signup">Signup</a>
              </li>
              <% } %>

              <% if (myUsr) { %>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
              </li>
              <% } %>
            </ul>
          </div>
        </div>
      </div>
    </nav>
  </body>
</html>
